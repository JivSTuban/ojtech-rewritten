# Server Configuration
server.port=${SERVER_PORT}

# PostgreSQL Database Configuration (Development)
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}
spring.datasource.driver-class-name=${DATABASE_DRIVER}

# H2 Database Configuration (Alternative for local development)
# spring.datasource.url=jdbc:h2:mem:ojtech;DATABASE_TO_UPPER=false;MODE=PostgreSQL;CASE_INSENSITIVE_IDENTIFIERS=true;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console
# spring.h2.console.settings.trace=true

# JPA Configuration (PostgreSQL)
spring.jpa.hibernate.ddl-auto=${JPA_HIBERNATE_DDL_AUTO}
spring.jpa.show-sql=${JPA_SHOW_SQL}
spring.jpa.properties.hibernate.format_sql=${JPA_FORMAT_SQL}
spring.jpa.database-platform=${JPA_DATABASE_PLATFORM}
spring.jpa.defer-datasource-initialization=${JPA_DEFER_DATASOURCE_INIT}


# Make hibernate use same case for table names
spring.jpa.hibernate.naming.physical-strategy=${JPA_PHYSICAL_NAMING_STRATEGY}
spring.jpa.hibernate.naming.implicit-strategy=${JPA_IMPLICIT_NAMING_STRATEGY}

# Disable globally quoted identifiers to avoid case sensitivity issues
spring.jpa.properties.hibernate.globally_quoted_identifiers=${JPA_GLOBALLY_QUOTED_IDENTIFIERS}

# Disable SQL initialization
spring.sql.init.mode=${SQL_INIT_MODE}

# JWT Configuration
app.jwt.secret=${JWT_SECRET}
app.jwt.expiration=${JWT_EXPIRATION}
app.jwt.header=${JWT_HEADER}
app.jwt.prefix=${JWT_PREFIX}

# OAuth2 Configuration (Legacy - maintained for backward compatibility)
app.security.oauth2.grant_types.password=${OAUTH2_GRANT_TYPE_PASSWORD}
app.security.oauth2.grant_types.implicit=${OAUTH2_GRANT_TYPE_IMPLICIT}
app.security.oauth2.grant_types.authorization_code=${OAUTH2_GRANT_TYPE_AUTH_CODE}
app.security.oauth2.grant_types.refresh_token=${OAUTH2_GRANT_TYPE_REFRESH}
app.security.oauth2.access_token_validity_seconds=${OAUTH2_ACCESS_TOKEN_VALIDITY}
app.security.oauth2.refresh_token_validity_seconds=${OAUTH2_REFRESH_TOKEN_VALIDITY}
app.security.oauth2.client1.id=${OAUTH2_CLIENT1_ID}
app.security.oauth2.client1.password=${OAUTH2_CLIENT1_PASSWORD}
app.security.oauth2.client1.scopes=${OAUTH2_CLIENT1_SCOPES}
app.security.oauth2.client2.id=${OAUTH2_CLIENT2_ID}
app.security.oauth2.client2.password=${OAUTH2_CLIENT2_PASSWORD}
app.security.oauth2.client2.scopes=${OAUTH2_CLIENT2_SCOPES}

# Cloudinary Configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}
cloudinary.api-secret-preset=${CLOUDINARY_API_SECRET_PRESET}

# File Upload Configuration
spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE}
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE}
file.upload-dir=${FILE_UPLOAD_DIR}

# OpenAPI/Swagger Configuration
springdoc.api-docs.path=${SPRINGDOC_API_DOCS_PATH}
springdoc.swagger-ui.path=${SPRINGDOC_SWAGGER_UI_PATH}
springdoc.swagger-ui.operationsSorter=${SPRINGDOC_OPERATIONS_SORTER}

# Logging Configuration
logging.level.org.springframework.security=${LOGGING_LEVEL_SPRING_SECURITY}
logging.level.com.ojtechapi.spring.jwtoauth=${LOGGING_LEVEL_APP}

# Google OAuth2 Configuration
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=${GOOGLE_OAUTH_SCOPE}

# GitHub OAuth2 Configuration   
spring.security.oauth2.client.registration.github.client-id=${GITHUB_CLIENT_ID}
spring.security.oauth2.client.registration.github.client-secret=${GITHUB_CLIENT_SECRET}
spring.security.oauth2.client.registration.github.scope=${GITHUB_OAUTH_SCOPE}
spring.security.oauth2.client.registration.github.redirect-uri=${GITHUB_REDIRECT_URI}

# Gemini API Configuration
gemini.api.key=${GEMINI_API_KEY}

# Email Configuration
# Set to false to disable email sending (useful for local development)
email.enabled=${EMAIL_ENABLED}

# Brevo API Configuration
brevo.api.key=${BREVO_API_KEY}
brevo.api.url=${BREVO_API_URL}

# Verified sender email address (must be verified in Brevo)
spring.mail.email=${SPRING_MAIL_EMAIL}

# Application Base URL
app.base-url=${APP_BASE_URL}
backend.base-url=${BACKEND_BASE_URL}
# backend.base-url=http://localhost:8080

# Frontend URL (for CV viewer and other frontend links in emails)
frontend.base-url=${FRONTEND_BASE_URL}

# frontend.base-url=http://localhost:5173